//
//  ItemUnitTest.swift
//  MyFitZ
//
//  Created by Andre Villanueva on 6/8/16.
//  Copyright © 2016 Bang Bang Studios. All rights reserved.
//

import XCTest

class ItemUnitTest: XCTestCase {
//    var item = Item()
    
    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }

    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }

    func testPerformanceExample() {
        // This is an example of a performance test case.
        self.measureBlock {
            // Put the code you want to measure the time of here.
        }
    }
    
    func testItemWorn(){
    }
    
    func testBlankItemUpdated(){
//        item = Item.init(make: "N/A", model: "N/A", category: "N/A", subCategory: "N/A", payedPrice: 0.0, Image: UIImage.init(named: "8's"), favorited: false, isThisNew: false, timesWorn: 1, lastTImeWorn: "12/12/12", kind: "N/A", size: "N/A", index: 1, datePurchased: "12/12/12", color: "N/A", dateDeleted: "N/A")
//        
//        guard let make = item.brand, model = item.model, category = item.category, subCategory = item.subCategory, payedPrice = item.payedPrice, image = item.image, favorited = item.favorited, isThisNew = item.isThisNew, timesWorn = item.timesWorn, lastTimeWorn = item.lastTimeWorn, kind = item.kind, size = item.size, index = item.index, datePurchased = item.datePurchased, color = item.color, daateDeleted = item.dateDeleted else{
//            XCTAssert(false, "One of the properties was not set for item: \(item.description)")
//        }
//            XCTAssert(true)
//        
    }

}
